# 自动转换所有文本文件的行尾为 LF（入库），检出时根据操作系统转换为合适的行尾
* text=auto

# Kotlin 源文件强制使用 LF
*.kt text eol=lf

# Markdown 文件也统一使用 LF
*.md text eol=lf

# 脚本文件（如果有）保持 LF
*.sh text eol=lf
*.py text eol=lf

# 二进制文件不要转换
*.png binary
*.jpg binary
*.ico binary
*.so binary
*.dll binary
